All checks complete. Here is my report.

---

## Methodology review — round 2 (revised manuscript, commit 27e1b09)

**Manuscript:** `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/paper/index.qmd`

### Recommendation: Major revisions (narrowly — two blocking items, both quickly fixable; the rest of the round-1 program is verifiably done)

The exposition the revision was asked to supply is now largely in place and, where I checked it against code and artifacts, correct: the hurdle/quantile/tail/smear description matches the implementation, the EB shrinkage formula in the text matches `_shrink_state_factors` line-for-line, the calibration table now quotes the matched frozen pair, and the round-1 direction-of-effect errors (coverage sign, SMD orientation) are fixed exactly per the committed artifacts. What blocks acceptance is (1) the headline simulation artifact cannot be regenerated from any committed code, while the manuscript claims the correction is done, and (2) a threshold-composition fact I recomputed from the raw file that materially reweights the headline decomposition number.

### Verification receipts (what I recomputed)

- **The 305 count and the sub-threshold claim (task item).** From the raw FY2024 PUF (`~/.cache/axiom-oracles/snap-qc/qc_pub_fy2024.csv`), positional counts for Colorado (STATE=8): 856 rows total, all CASE==1; 305 with STATUS ∈ {2,3}; **195 of the 305 have AMTERR ≤ 56** and 110 have AMTERR ≥ 57 (no zeros, blanks, decimals, or negatives; all 551 STATUS-1 rows have AMTERR = 0). The paper's parenthetical "this count includes deviations below the official threshold" is **true — and dramatically understated: 64% of the decomposition universe is sub-threshold.**
- **Replay heterogeneity (consequence of the above).** From `paper/snapshot/labs/amterr/amterr_replay_results.json` (283 filtered cases = 97 above + 186 below threshold): within-$5 explanation is 76/97 = **78.4% among above-threshold (official) errors** vs 170/186 = **91.4% among sub-threshold deviations**; blended 246/283 = 86.9% as published. 33 of the 246 "explained" cases have AMTERR ≤ $5 — mechanically explained at the $5 comparison tolerance regardless of solver correctness. Also verified: the solver–engine partition identity holds exactly (zero cases with `within5` ⊕ `solver_within5`).
- **Boundary table (@tbl-boundary) and audit-volume figures.** All verified against `paper/snapshot/labs/results_by_state_corrected.json`: ND 49.5%→"50", WA 48.3→"48", CO 48.6→"49", KS 46.8→"47", NV 46.0→"46"; CO $156.4M/$32.8M/$63.4M step; MO +$3.90M, TN +$3.69M, IN +$3.35M, CA −$30.26M, PA −$13.13M, TX −$13.06M; CA SD −$43.8M. The round-1 F1-traceability defect is resolved *relative to this artifact*.
- **Model-section numbers.** AUC 0.7609→0.7666 (+0.0057/+0.0030), 47.8% at 13.4% base, smear 1.1725, $183.35 vs $189.30, sign AUC 0.6996 (calibrated, labeled as such), coverage gaps −0.30…−3.47 all negative with 7/9 within 3pp, concordance 99.99689%/83.64% — all match `analysis/FINDINGS.md` and the JSONs. Contrast paragraph numbers (2.60/2.97; AZ +2.07/+0.64; KY −0.42; CA +0.11/−1.40; LA/MI within 0.2; single-digit events) all match.

### Major issues

**1. The corrected simulation artifact has no committed generator, and the submission's own code contradicts the manuscript's two "fixed" claims.** FACTS F1 labels `results_by_state_corrected.json` "(official error gate, CASE==1)", and the artifact's schema, seed (11), draw count (100k), and indent match exactly one script — `examples/all_states.py` — whose output is **gitignored** and which runs through `snap_qc_sim/data.py`, where the round-1 banned gate is still present verbatim at this commit:

```python
counted = (
    (row.get("STATUS") or "").strip() in ("2", "3")
    and correct is not None
    and abs(issued - correct) > threshold   # correct = FSBEN — the banned benefit-difference gate
)
```
(`snap_qc_sim/data.py:90–94`, no CASE==1 filter.) `scripts_build_data.py` builds the app data from this loader; the shipped `app/public/data.json` still contains **44,891** cases (I summed the 53 per-state `n` values — full file, not the 44,800 official universe); the snapshot lab script `paper/snapshot/labs/mc_tool.py` carries the same old gate. So rerunning the committed code reproduces the *banned-gate* numbers, and the one artifact carrying F1–F4 is irreproducible from the repository. Meanwhile §sec-simulate states the deployed version is "now corrected," and §sec-model states "the simulator's model-based mode is disabled pending those fixes" — but `app/public/app.js` in this submission has a live, user-selectable model mode (`main()`, lines 455–481, fetching `model_data.json`). For a paper whose stated method is that "a fact catalog maps every quantitative claim to a committed artifact" and whose reruns are "byte-for-byte," this is the central defect: commit the corrected loader (gate on `AMTERR > 56`, filter `CASE == 1`), rebuild `data.json`, regenerate the artifact from committed code, and make the two "now corrected/disabled" sentences true of the repository being reviewed. (Note the fix barely moves the numbers — CO 48.5→48.6%, levers $606M→$609M — which is worth one sentence of robustness disclosure.)

**2. The threshold composition of the decomposition universe is material and unreported.** Given 195/305 sub-threshold and the 78.4%-vs-91.4% split above: the abstract's "explains 86.9% of filtered Colorado error cases" will be read as official error cases, for which the correct figure is **78.4%** (binomial SE ≈ 4.2pp), and the computation-side upper bound among official errors is **21.6%, not 13.1%** — roughly double. Downstream: the $112.6M "weighted error dollars" and the layer-1/-2 percentage denominators include sub-threshold dollars the official rate excludes entirely (order of magnitude ~a fifth of the total; compute it exactly), so the "file-derived 8.88%" is not an official-definition rate — it is *inflated* by sub-threshold dollars while *deflated* by the ineligible-case truncation, and the §sec-qc sentence "part of why 8.88% sits below 9.97%" understates the definitional gap. Required: report the 110/195 split and the by-group explanation rates; either restrict headline layer-3 numbers to above-threshold errors or label the blend explicitly; and state the official-gate file-derived rate alongside 8.88%.

**3. The truncation direction claim is right, but the paper signs the *total* with one component (task item).** My assessment of "it biases simulated sampling variability downward": **correct as applied, though not a theorem.** One-sided truncation can raise conditional variance in pathological mixtures, but here the excluded cases (1,037 ineligible + 406 full-overissuance) carry per-case error contributions at the extreme top — the full issued benefit, mean ≈ $400+/case-month against an in-file mean per-case error contribution of ≈ $25–30 — so restoring them adds a between-component variance term q(1−q)(μ_G−μ_file)² that dwarfs both the (1−q) dilution and the ~3% offsetting denominator-issuance effect at any plausible parameterization; the additive recentering restores the level but not the spread. The paper should give this one-sentence argument rather than assert the direction. The real problem is the inferential leap in §sec-qc: "meaning the tier-noise results of @sec-simulate are, if anything, understated." Three approximations affect the SD: truncation (downward — correct), the re-review adjustment as a fixed additive level (downward, doubly so for high-adjustment states, where a multiplicative correction would scale the resampled numerator), and the **i.i.d. approximation to the monthly-systematic stratified design of 7 C.F.R. 275.11, which plausibly biases the simulated SD *upward*** (proportionate time-stratification generally reduces design variance). Two down, one up: "if anything, understated" is a judgment presented as a consequence. Either bracket it (a month-stratified bootstrap is cheap; YRMONTH is on the file) or weaken to "likely understated" and name the countervailing component. Round-1's request for any reconciliation against a design-based SE remains unmet; the revision substitutes correct scope sentences for the analysis.

### Minor issues

1. **@tbl-validate correlation cell:** "0.51" matches neither the frozen-model unfactored equal-jurisdiction correlation (0.518 → 0.52) nor cleanly anything but the *primary* model's 0.507 — reintroducing, in one cell, the mismatched-pair defect the table was rebuilt to remove. Print 0.52 (or three decimals) and say which weighting the row uses (equal vs issuance differ: 0.518/0.459).
2. **EB exposition gap:** the formula is now correct and code-matching, but τ² estimation (Kish-effective-n-weighted method of moments about the fixed prior mean 1, floored at 0; `hurdle_deviation_model.py:782–787`) is unstated, and factor uncertainty is still not propagated into FY2024 (round-1 Minor 8). One sentence each.
3. **Hurdle composition unstated:** the text lists "a deviation probability... and an above-threshold probability" without saying stage 2 is fit and applied *among stage-1 deviators* and that the official-error probability is their product, nor how rare |D| ≤ $0.50 official errors are handled (FINDINGS states both).
4. **Tail description names the attachment depth (q99) but not the fit construction** (mean excess of OOF median-model residuals over *their own* q90 — `distributional_deviation_model.py:633`). Given the disclosed depth mismatch, name both depths; the "overstating extreme-tail mass" direction is plausible (decreasing mean excess in logs) but no committed diagnostic (threshold-stability at q85/q90/q95) evidences it. σ = 0.467 sitting just under the 0.5 finite-variance boundary is still unmentioned. Since the model mode is quoted nowhere, an alternative is to trim the tail sentence to a one-line disclosure.
5. **`simulate.py` recentering can produce negative-rate draws** for low-rate states (no clip; harmless for tier probabilities, not for quoted rate SDs) — round-1 Minor 9, unaddressed.
6. **Loader hygiene:** `_num(row.get("HWGT")) or 0.0` retains zero-weight rows in bootstrap slots (round-1 Minor 10) — moot after the CASE==1 rebuild, but fix while touching the gate.
7. **§sec-qc's recording rule** ("when they differ by more than a tolerance threshold — a payment error with coded findings") still reads as if findings exist only above tolerance; the file codes findings for CO's 195 sub-threshold cases. One reconciling clause.
8. **F4 ($609M/$1,310M)** is a 53-state sum I could not independently re-derive absent the committed generator (see Major 1); commit the summation or a per-state delta table.
9. **Issuance-base wobble:** `native_decomposition.json` CO `benefit_dollars` $1.255B vs the quoted $1.268B issuance (the corrected artifact's figure) — label which aggregate the 8.88% uses.

### Strengths

1. **The round-1 corrections are real and exact.** Coverage now reported as one-sided under-coverage with the right sign and magnitude, connected to the $183-vs-$189 comparison; the SMD cross-section is correctly oriented with the mixed-cell composition stated and the endogeneity and bundling caveats attached; the Kentucky dissolution is disclosed. Every number I checked in these passages matches its artifact.
2. **The descriptive-contrast paragraph now stands.** Both denominators, calendar alignment, unweighted event counts acknowledged as single-digit, "contrasts of policy bundles," and the explicit anti-narrative reading ("runs against a simple burden-increases-error reading") — this is model disclosure practice well above field norm.
3. **The EB shrinkage description is now a faithful rendering of careful code** — delta-method ratio variance with Kish effective-n, small-sample correction, variance floor — and the "most cross-state alignment comes from the factors, whose interpretation the data do not identify" sentence is exactly the right epistemic posture.
4. **The scope architecture (§sec-oracle-scope, the four §sec-qc framing paragraphs, the limitations section) is unusually complete and, where checkable, accurate.** My independent recomputations — CO 856/305, NY 885 = CASE==1, the 246/37 partition identity with zero solver–engine disagreements, STATUS-1 ⇒ AMTERR = 0 — all confirmed the underlying artifact claims.
5. **The oracle contribution survives scrutiny.** The zero-tolerance parity target with the C2-style scoping (agreement with the agency's computational canon from supplied intermediates, not independent adjudication) remains the paper's genuine advance over cross-model validation, and the fiscal-2025 confirmatory commitments are the right pre-registration instinct.

### What a methods journal would still demand, ranked

1. Reproducibility of F1–F4 from committed code (Major 1) — corrected gate in `snap_qc_sim/data.py`, rebuilt `data.json`, regenerated artifact, reconciled prose.
2. The 110/195 threshold split and by-group replay rates (Major 2) — this changes a headline number's interpretation.
3. Design-fidelity bracketing or a softened total-sign claim (Major 3) — month-stratified bootstrap, re-review variance term, multiplicative-vs-additive anchoring.
4. The one-sentence truncation-direction argument (currently asserted).
5. Tail diagnostics or a trimmed tail claim; τ² and hurdle-composition sentences.
6. The scattered minors (correlation cell, negative draws, zero weights, F4 summation).

Key files: `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/paper/index.qmd`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/snap_qc_sim/data.py`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/app/public/data.json`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/examples/all_states.py`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/paper/snapshot/labs/results_by_state_corrected.json`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/paper/snapshot/labs/amterr/amterr_replay_results.json`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/analysis/hurdle_deviation_model.py` (EB: lines 764–806), `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/analysis/FINDINGS.md`, `/Users/maxghenis/PolicyEngine/_worktrees/snap-qc-paper/paper/FACTS.md`.