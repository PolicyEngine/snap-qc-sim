# Round-1 resolutions (maintainer adjudication, 2026-08-16)

Reviewer: gpt-5.6-sol, adversarial, read-only; six reports + summary
archived unedited beside this file. Every number survived verification;
the findings were prose. Dispositions:

1. **Blocker — "never-migrated states" unsupported.** CONFIRMED, fixed at
   every site: donors are "states with no recorded migration in the
   paper's event registry" (abstract, introduction, conclusion).
2. **Blocker — abstract made descriptive channels inferential.**
   CONFIRMED, fixed: the abstract now names mass change and information
   disregarded as the inferential channels, states both estimators'
   effects, and says worker and data-entry error fall below the count
   gate and are reported descriptively.
3. **Blocker — "the years FNS billed" ≠ FY2017–19.** CONFIRMED, fixed
   everywhere: FY2017–19 is "the pre-named consequence window inside the
   September 2016 to December 2019 interval for which FNS later billed."
4. **Major — "same mass-change signal" privileged the fixed-donor
   estimator.** CONFIRMED, fixed: every such sentence now says the two
   estimators return the same p-value and rank and differ only in the
   parent-rule verdict field, and why; the abstract states both effects
   and the verdict split explicitly.
5. **Major — mechanism glosses in the introduction.** CONFIRMED (the
   maintainer's own exemplar), fixed: "a system that computed wrong /
   workers who could not drive it / died in a queue / batch actions
   computed wrong at scale" replaced by the code labels and "as the QC
   record classified it."
6. **Major — "one signal, one null, one refusal" broke the verdict
   taxonomy.** CONFIRMED, fixed in introduction and conclusion: one
   `signal`, two `no_protocol_defined_signal`, the second a refusal by
   the placebo test.
7. **Major — reproducibility map missing.** CONFIRMED, fixed: new
   "Artifacts and reproduction" section anchors every artifact,
   generator, protocol (with SHA-256 prefixes), and test file, states
   the environment command, the raw-cache dependency, the skip
   behavior, and the repository commit (4aafd06).
8. **Major — introduction previews then sections repeat.** CONFIRMED,
   fixed by rewriting the introduction to state the question, events,
   estimand, design shape, and verdict words only; all numbers now live
   in the sections that own them.
9. **Major — unresolved citations.** CONFIRMED, held open: the
   `[NEEDS CITATION]` markers stay in the text by design (public-use
   file definitions and tolerance values are resolvable from the
   techdocs in the local cache and will be resolved in round 2 with
   page cites; registry gaps and element-code labels are genuinely
   open).
10. **Minor — language.** Process narration and orientation openers
    removed ("What follows…", "worth stating first rather than last",
    three section first-sentences); the abstract's em-dash chain
    removed; "outright" and "documented as troubled" replaced with the
    registry's recorded facts. Systematic passive constructions remain
    partially addressed and are the round-2 language target.
